# 🔄 ПЛАН КОНВЕРТАЦИИ REACT → OPENCART PHP

**Дата:** 20 ноября 2025
**Задача:** Конвертировать React-версию сертификатов в OpenCart .tpl шаблон

---

## 1. 🎯 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

### Что есть сейчас:

**React-версия (GitHub Pages):**
- ✅ 155 React компонентов
- ✅ 24 страницы (SPA)
- ✅ Vite + TypeScript
- ✅ Работает на https://gringo999max.github.io/Certificates20112025/
- ❌ Не интегрирована с OpenCart
- ❌ Нет серверной части

**Старая версия (.tpl):**
- ✅ Есть контроллер и базовая структура
- ✅ Интеграция с Тинькофф оплатой
- ✅ Форма заказа сертификатов
- ✅ Работает на глэмпинги.рф/gift_certificate
- ❌ Устаревший дизайн
- ❌ Ограниченный функционал

### Что нужно получить:

**Новая версия (гибрид):**
- ✅ Дизайн из React-версии
- ✅ Функционал из React-версии
- ✅ Интеграция с OpenCart
- ✅ PHP-шаблоны (.tpl)
- ✅ Работа на глэмпинги.рф/certificates

---

## 2. 📋 СТРУКТУРА ФАЙЛОВ OPENCART

### Необходимая структура:

```
catalog/
├── controller/
│   └── information/
│       └── certificates.php              ← Контроллер страницы
│
├── view/
│   └── theme/default/
│       ├── template/
│       │   └── information/
│       │       ├── certificates.tpl      ← Главный шаблон
│       │       └── blocks/
│       │           ├── hero.tpl          ← Hero секция
│       │           ├── catalog.tpl       ← Каталог сертификатов
│       │           ├── how_it_works.tpl  ← Как это работает
│       │           ├── reviews.tpl       ← Отзывы (уже есть)
│       │           ├── glampings.tpl     ← Глэмпинги (уже есть)
│       │           ├── faq.tpl           ← FAQ
│       │           ├── delivery.tpl      ← Доставка
│       │           └── payment.tpl       ← Оплата
│       │
│       └── stylesheet/
│           └── static/
│               └── certificates.css      ← Стили
│
└── javascript/
    └── informations/
        └── certificates.js               ← JavaScript
```

---

## 3. 🔧 ЭТАПЫ КОНВЕРТАЦИИ

### Этап 1: Подготовка (СЕГОДНЯ)

**1.1. Извлечь из React-версии:**
- [ ] Все секции как отдельные HTML-блоки
- [ ] Все стили в единый CSS
- [ ] Все скрипты в единый JS
- [ ] Все изображения (оптимизированные)

**1.2. Создать структуру OpenCart:**
- [ ] Контроллер certificates.php
- [ ] Главный шаблон certificates.tpl
- [ ] Блоки (hero, catalog, how_it_works, faq, etc.)
- [ ] CSS файл
- [ ] JS файл

**1.3. Интегрировать с существующей системой:**
- [ ] Подключить header/footer из OpenCart
- [ ] Подключить форму заказа (из gift_certificate.tpl)
- [ ] Подключить breadcrumbs
- [ ] Подключить существующие блоки reviews.tpl, glampings.tpl

---

### Этап 2: Конвертация компонентов (СЕГОДНЯ/ЗАВТРА)

**2.1. Hero секция:**
```php
<div class="certificates-hero">
    <div class="hero-content">
        <h1>Подарочные сертификаты на глэмпинги</h1>
        <p>500+ локаций по всей России</p>
        <a href="#catalog" class="btn-primary">Выбрать сертификат</a>
    </div>
    <div class="hero-image">
        <img src="image/catalog/certificates/hero.webp" alt="Глэмпинги">
    </div>
</div>
```

**2.2. Каталог сертификатов:**
```php
<div id="catalog" class="certificates-catalog">
    <h2>Выберите подарочный сертификат</h2>
    <div class="certificates-grid">
        <?php foreach ($certificates as $cert) { ?>
            <div class="certificate-card">
                <img src="<?php echo $cert['image']; ?>" alt="<?php echo $cert['name']; ?>">
                <h3><?php echo $cert['name']; ?></h3>
                <p class="price"><?php echo $cert['price']; ?> ₽</p>
                <button onclick="selectCertificate(<?php echo $cert['id']; ?>)">Выбрать</button>
            </div>
        <?php } ?>
    </div>
</div>
```

**2.3. "Как это работает":**
```php
<div id="how-it-works" class="how-it-works-section">
    <h2>Как это работает?</h2>
    <div class="steps-grid">
        <div class="step">
            <div class="step-number">1</div>
            <h3>Выберите сертификат</h3>
            <p>Выберите номинал и дизайн</p>
        </div>
        <!-- Шаги 2, 3, 4 -->
    </div>
</div>
```

**2.4. FAQ:**
```php
<div id="faq" class="faq-section">
    <h2>Частые вопросы</h2>
    <div class="faq-list">
        <?php foreach ($faqs as $faq) { ?>
            <div class="faq-item">
                <div class="faq-question"><?php echo $faq['question']; ?></div>
                <div class="faq-answer"><?php echo $faq['answer']; ?></div>
            </div>
        <?php } ?>
    </div>
</div>
```

---

### Этап 3: Стили и скрипты (СЕГОДНЯ/ЗАВТРА)

**3.1. CSS (certificates.css):**
- Извлечь все стили из React-версии
- Адаптировать под OpenCart структуру
- Минифицировать
- Добавить responsive стили

**3.2. JavaScript (certificates.js):**
```javascript
// Замена React функционала на jQuery
$(document).ready(function() {
    // Плавный скролл к секциям
    $('a[href^="#"]').click(function() {
        var target = $(this.hash);
        $('html, body').animate({
            scrollTop: target.offset().top
        }, 800);
        return false;
    });

    // FAQ аккордеон
    $('.faq-question').click(function() {
        $(this).next('.faq-answer').slideToggle();
    });

    // Выбор сертификата
    window.selectCertificate = function(id) {
        // Открыть модальное окно с формой
        $('#buy_form').find('#certificate-id').val(id);
        $.magnificPopup.open({
            items: { src: '#buy_form' },
            type: 'inline'
        });
    };
});
```

---

### Этап 4: Оптимизация (СЛЕДУЮЩАЯ НЕДЕЛЯ)

**4.1. Изображения:**
- [ ] Конвертировать PNG → WebP
- [ ] Сжать через TinyPNG
- [ ] Создать responsive версии (mobile, tablet, desktop)
- [ ] Добавить lazy loading

**4.2. Производительность:**
- [ ] Минифицировать CSS
- [ ] Минифицировать JS
- [ ] Объединить файлы
- [ ] Добавить кэширование

**4.3. SEO:**
- [ ] Meta теги для страницы
- [ ] Правильные H1-H6
- [ ] Alt теги для изображений
- [ ] Schema.org разметка

---

## 4. 🗑️ ЧТО УДАЛИТЬ ИЗ REACT-ВЕРСИИ

### НЕ переносить в .tpl:

**Компоненты:**
- ❌ DevNavigationButton.tsx
- ❌ Все *TestPage.tsx (10 файлов)
- ❌ Все *Variants.tsx (8 файлов)
- ❌ ErrorBoundary, Suspense (не нужны в PHP)
- ❌ CartContext, state management (серверный рендеринг)

**Файлы:**
- ❌ 98 MD-файлов документации
- ❌ package.json, vite.config.ts
- ❌ TypeScript типы
- ❌ React Router

**Что ПЕРЕНЕСТИ:**
- ✅ Дизайн и верстка всех основных страниц
- ✅ Стили (CSS)
- ✅ Изображения (оптимизированные)
- ✅ Логика форм (адаптировать под jQuery)
- ✅ Анимации (адаптировать под jQuery)

---

## 5. 🔄 ИНТЕГРАЦИЯ С OPENCART

### 5.1. Контроллер (certificates.php):

```php
<?php
class ControllerInformationCertificates extends Controller {
    public function index() {
        $this->load->language('information/certificates');

        $data['breadcrumbs'] = array();
        $data['breadcrumbs'][] = array(
            'text' => $this->language->get('text_home'),
            'href' => $this->url->link('common/home')
        );
        $data['breadcrumbs'][] = array(
            'text' => 'Подарочные сертификаты',
            'href' => $this->url->link('information/certificates')
        );

        // Загрузка данных сертификатов
        $this->load->model('catalog/certificate');
        $data['certificates'] = $this->model_catalog_certificate->getCertificates();

        // Загрузка FAQ
        $data['faqs'] = $this->model_catalog_certificate->getFAQs();

        // Загрузка отзывов
        $this->load->model('catalog/review');
        $data['reviews'] = $this->model_catalog_review->getReviews();

        $data['header'] = $this->load->controller('common/header');
        $data['footer'] = $this->load->controller('common/footer');

        $this->response->setOutput($this->load->view('information/certificates', $data));
    }
}
```

### 5.2. Модель (certificate.php):

```php
<?php
class ModelCatalogCertificate extends Model {
    public function getCertificates() {
        $query = $this->db->query("
            SELECT * FROM " . DB_PREFIX . "certificate
            WHERE status = 1
            ORDER BY sort_order ASC
        ");
        return $query->rows;
    }

    public function getFAQs() {
        $query = $this->db->query("
            SELECT * FROM " . DB_PREFIX . "certificate_faq
            ORDER BY sort_order ASC
        ");
        return $query->rows;
    }
}
```

### 5.3. База данных:

```sql
CREATE TABLE IF NOT EXISTS `oc_certificate` (
    `certificate_id` int(11) NOT NULL AUTO_INCREMENT,
    `name` varchar(255) NOT NULL,
    `description` text,
    `price` decimal(15,4) NOT NULL,
    `image` varchar(255),
    `sort_order` int(3) DEFAULT 0,
    `status` tinyint(1) DEFAULT 1,
    PRIMARY KEY (`certificate_id`)
);

CREATE TABLE IF NOT EXISTS `oc_certificate_faq` (
    `faq_id` int(11) NOT NULL AUTO_INCREMENT,
    `question` text NOT NULL,
    `answer` text NOT NULL,
    `sort_order` int(3) DEFAULT 0,
    PRIMARY KEY (`faq_id`)
);
```

---

## 6. 🎨 АДАПТАЦИЯ ДИЗАЙНА

### 6.1. Сохранить из React:
- ✅ Цветовая схема (emerald/green)
- ✅ Типографика (шрифты)
- ✅ Компоновка секций
- ✅ Анимации (адаптировать)
- ✅ Responsive дизайн

### 6.2. Адаптировать под OpenCart:
- Header/Footer из текущей темы
- Breadcrumbs в стиле сайта
- Кнопки в стиле сайта
- Формы в стиле сайта

---

## 7. 📱 МОБИЛЬНАЯ ВЕРСИЯ

### Обязательно:
- [ ] Hamburger меню
- [ ] Touch-friendly кнопки
- [ ] Адаптивные изображения
- [ ] Swipe для каруселей
- [ ] Оптимизация форм

---

## 8. ✅ ЧЕКЛИСТ ПЕРЕД ПЕРЕДАЧЕЙ ТИМУРУ

### Файлы:
- [ ] certificates.php (контроллер)
- [ ] certificates.tpl (главный шаблон)
- [ ] Блоки .tpl (hero, catalog, how_it_works, etc.)
- [ ] certificates.css (стили)
- [ ] certificates.js (скрипты)
- [ ] Оптимизированные изображения

### Функционал:
- [ ] Все секции отображаются
- [ ] Форма заказа работает
- [ ] FAQ аккордеон работает
- [ ] Плавный скролл работает
- [ ] Мобильная версия корректна

### Интеграция:
- [ ] Header/Footer OpenCart подключены
- [ ] Breadcrumbs работают
- [ ] Ссылки на каталог глэмпингов
- [ ] Интеграция с оплатой
- [ ] База данных создана

### Документация:
- [ ] Инструкция по установке
- [ ] Описание структуры файлов
- [ ] SQL скрипты для БД
- [ ] Комментарии в коде

---

## 9. 🚀 ПЛАН ТЕСТИРОВАНИЯ

### Локальное тестирование:
1. Поднять на тестовом домене
2. Проверить все секции
3. Проверить формы
4. Проверить на разных устройствах

### Продакшн:
1. Залить на глэмпинги.рф/certificates
2. Финальное тестирование
3. Мониторинг ошибок

---

## 10. 📊 СРАВНЕНИЕ: ДО И ПОСЛЕ

| Параметр | React (было) | OpenCart .tpl (будет) |
|----------|--------------|------------------------|
| Технология | React + Vite | PHP + jQuery |
| Размер | 55 МБ bundle | ~2-3 МБ |
| Загрузка | 3-5 сек | 1-2 сек |
| SEO | Плохо (SPA) | Отлично (SSR) |
| Интеграция | Нет | Полная |
| Редактирование | Код | Админка |

---

## 11. 🎯 ПРИОРИТЕТЫ

### 🔴 КРИТИЧНО (сделать сегодня):
1. Создать структуру файлов OpenCart
2. Конвертировать главные секции (Hero, Catalog)
3. Подключить существующую форму заказа
4. Базовые стили

### 🟡 ВАЖНО (на этой неделе):
5. Все остальные секции (How it works, FAQ, Reviews)
6. Полная стилизация
7. JavaScript функционал
8. Тестирование

### 🟢 МОЖНО ОТЛОЖИТЬ:
9. Оптимизация изображений
10. Админ-панель для управления
11. Расширенная аналитика

---

**Итого:** Задача - конвертировать React SPA в OpenCart PHP-шаблоны с сохранением дизайна и функционала, но с интеграцией в существующую систему.
